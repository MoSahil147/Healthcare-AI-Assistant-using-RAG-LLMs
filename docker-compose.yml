version: "3.9"

services:
  healthcare-ai:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./store:/app/store
    env_file:
      - .env
    restart: unless-stopped
